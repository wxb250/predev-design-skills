interface:
  display_name: "Multi-Session Project Coordinator"
  short_description: "Coordinate long-running Codex worker sessions, worktrees, approvals, heartbeats, and integration gates."
  default_prompt: "Use $multi-session-project-coordinator to decide whether this project should use multi-session coordination, then supervise worker threads if enabled."
policy:
  allow_implicit_invocation: true
